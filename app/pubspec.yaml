name: app
description: A new Flutter application.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^1.0.1
  firebase_storage: ^8.0.0
  firebase_auth: ^1.0.1
  provider: ^5.0.0
  firebase_core: ^1.0.1

  fl_chart: ^0.20.1
  new_gradient_app_bar: ^0.1.5

  google_maps_flutter: ^2.0.1
  location: ^4.1.1

  quill_delta: ^2.0.0
  google_maps_place_picker:
    git: git@github.com:fysoul17/google_maps_place_picker.git

  zefyr:
    git:
      url: git@github.com:eymarkfr/zefyr.git
      path: packages/zefyr
      ref: 1.0-dev

  fluster:
    git:
      url: git@github.com:eymarkfr/fluster.git

  fluttertoast: ^7.1.6
  image_picker: ^0.7.3
  image: ^3.0.2

  # Cache manager
  flutter_cache_manager:
    git:
      url: git@github.com:eymarkfr/flutter_cache_manager.git
      path: flutter_cache_manager


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  uses-material-design: true

  assets:
    - assets/images/

